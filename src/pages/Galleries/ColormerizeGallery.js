import React from 'react'
import { Gallery } from '../../components/Gallery/Gallery'

export const ColormerizeGallery = ({routeLocation}) => {
  return (
    <Gallery routeLocation={routeLocation}/> 
  )
}
