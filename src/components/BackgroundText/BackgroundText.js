import React from 'react';
import { BackgroundHeader } from './BackgroundTextElements';

const BackgroundText = ({ header }) => {
  return (
    <BackgroundHeader>{header}</BackgroundHeader>
  )
}

export default BackgroundText